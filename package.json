{
  "name": "clinic-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "诊所管理系统 - Cloudflare Pages 部署入口",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
